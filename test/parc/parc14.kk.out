module parc/parc14
private import std/core/types = std/core/types = "";
private import std/core = std/core = "";
public fun test : forall<a> (x : int, y : a, z : a) -> a
 = fn(x: int, y: 24, z: 24){
 match (x) {
 0
 -> std/core/types/.drop(x);
 std/core/types/.drop(z);
 y;
 _
 -> std/core/types/.drop(x);
 std/core/types/.drop(y);
 z;
 };
 };
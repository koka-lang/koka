public module sys/dom/html/barProp

// Interface BarProp (generated by esidl 0.2.0).

import sys/dom/types

/* safe and unsafe conversion from all types including `any */
external inline maybeBarProp  : forall<a> (a) -> maybe<barProp>
{ inline "(#1.toString()==='[object BarProp]'?std_core.Just(#1):std_core.Nothing)" }
external inline toBarProp : forall<a> (a) -> exn barProp
{ inline "(#1.toString()==='[object BarProp]'?#1:std_core.error('toBarProp: cast failed'))" }


// BarProp implements BarProp (supplemental)
external inline visible : (this: barProp) -> dom bool
{ inline "#1.visible" }
external inline visible : (this: barProp, assigned: bool) -> dom ()
{ inline "(#1.visible = #2)" }

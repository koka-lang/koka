public module sys/dom/nodeList

// Interface NodeList (generated by esidl 0.2.0).

import sys/dom/types

/* safe and unsafe conversion from all types including `any */
external inline maybeNodeList  : forall<a> (a) -> maybe<nodeList>
{ inline "(#1.toString()==='[object NodeList]'?std_core.Just(#1):std_core.Nothing)" }
external inline toNodeList : forall<a> (a) -> exn nodeList
{ inline "(#1.toString()==='[object NodeList]'?#1:std_core.error('toNodeList: cast failed'))" }


// NodeList implements NodeList (supplemental)
external inline item : (this: nodeList, index: uint32) -> dom node
{ inline "#1.item(#2)" }

external inline length : (this: nodeList) -> dom uint32
{ inline "#1.length" }

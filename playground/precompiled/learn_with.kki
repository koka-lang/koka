/* Shows `with` syntax examples.
   Prerequisites:
   - learn/basic
*/
module interface learn/with
 
//------------------------------
//#kki: import declarations
 
import std/core/types = std/core/types pub = "";
import std/core/hnd = std/core/hnd pub = "";
import std/core/exn = std/core/exn pub = "";
import std/core/bool = std/core/bool pub = "";
import std/core/order = std/core/order pub = "";
import std/core/char = std/core/char pub = "";
import std/core/int = std/core/int pub = "";
import std/core/vector = std/core/vector pub = "";
import std/core/string = std/core/string pub = "";
import std/core/sslice = std/core/sslice pub = "";
import std/core/list = std/core/list pub = "";
import std/core/maybe = std/core/maybe pub = "";
import std/core/maybe2 = std/core/maybe2 pub = "";
import std/core/either = std/core/either pub = "";
import std/core/result = std/core/result pub = "";
import std/core/tuple = std/core/tuple pub = "";
import std/core/lazy = std/core/lazy pub = "";
import std/core/show = std/core/show pub = "";
import std/core/debug = std/core/debug pub = "";
import std/core/delayed = std/core/delayed pub = "";
import std/core/console = std/core/console pub = "";
import std/core = std/core = "";
 
//------------------------------
//#kki: external imports
 
 
//------------------------------
//#kki: fixity declarations
 
 
//------------------------------
//#kki: local imported aliases
 
 
//------------------------------
//#kki: type declarations
 
// a dynamically bound value `ask`
recursive linear effect type ask[72,1,72,20] <(e :: E),a> :: (E, V) -> V {
  con @Hnd-ask[72,1,72,13](@cfc: std/core/types/int, @val-ask: (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>){0,2,8} : forall<(e :: E),a> (std/core/types/int, (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>) -> (ask :: (E, V) -> V)<(e :: E),a> = 1;
};
 
//------------------------------
//#kki: declarations
 
// monadic lift
fun @mlift-twice@10030[1,0,1,0] : forall<a,(e :: E)> (f : () -> (e :: E) a, wild_ : a) -> (e :: E) a;
pub fun twice[20,9,20,13] : forall<a,(e :: E)> (f : () -> (e :: E) a) -> (e :: E) a;
pub fun example1[25,9,25,16] : () -> <(std/core/console/console :: X)> ();
// The above is equivalent to:
pub fun example1-expanded[31,9,31,25] : () -> <(std/core/console/console :: X)> ();
// Without using layout, the fully expanded version is:
pub fun example1-fully-expanded[38,9,38,31] : () -> <(std/core/console/console :: X)> ();
pub fun example2[58,9,58,16] : () -> <(std/core/console/console :: X)> (std/core/types/list :: V -> V)<()>;
// which is equivalent to:
pub fun example2-expanded[64,9,64,25] : () -> <(std/core/console/console :: X)> (std/core/types/list :: V -> V)<()>;
// select `@val-ask` operation out of effect `:ask`
fun @val-ask/@select[72,12,72,20] : forall<(e :: E),a> (^ hnd : (ask :: (E, V) -> V)<(e :: E),a>) -> (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>;
// Call the `val ask` operation of the effect `:ask`
fun @val-ask[72,12,72,14] : () -> <(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>> std/core/types/int;
// Automatically generated. Retrieves the `@val-ask` constructor field of the `:ask` type.
fun ask/@val-ask[72,12,72,20] : forall<(e :: E),a> (^ ask : (ask :: (E, V) -> V)<(e :: E),a>) -> (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>;
// Automatically generated. Retrieves the `@cfc` constructor field of the `:ask` type.
fun ask/@cfc[72,1,72,13] : forall<(e :: E),a> (^ ask : (ask :: (E, V) -> V)<(e :: E),a>) -> std/core/types/int;
// runtime tag for the effect `:ask`
val ask/@tag[72,1,72,13] : (std/core/hnd/htag :: ((E, V) -> V) -> V)<(ask :: (E, V) -> V)>;
// handler for the effect `:ask`
fun ask/@handle[72,12,72,13] : forall<a,(e :: E),b> (hnd : (ask :: (E, V) -> V)<(e :: E),b>, ret : (res : a) -> (e :: E) b, action : () -> <(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>|(e :: E)> a) -> (e :: E) b;
// Call the `val ask` operation of the effect `:ask`
val ask[72,12,72,14] : (std/core/types/@valueop :: (E, V) -> V)<<(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>>,std/core/types/int>;
fun ask-twice[74,5,74,13] : () -> <(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>> std/core/types/int;
pub fun example3[77,9,77,16] : () -> <(std/core/console/console :: X)> ();
// Which is the same as:
pub fun example3-expanded[83,9,83,25] : () -> <(std/core/console/console :: X)> ();
pub fun example4[103,9,103,16] : () -> <(std/core/console/console :: X)> ();
pub fun example4-expanded[108,9,108,25] : () -> <(std/core/console/console :: X)> ();
pub fun main[113,9,113,12] : () -> <(std/core/console/console :: X)> ();
 
//------------------------------
//#kki: external declarations
 
 
//------------------------------
//#kki: inline definitions
 
//.inline-section
fun twice // inline size: 2
  = forall<a,(e :: E)> fn<(e :: E)>(f: () -> (e :: E) a){
    val _[21,3,21,5] : a
          = f();
    f();
  };
fun example1 // inline size: 4
  = fn<<(std/core/console/console :: X)>>(){
    val _@0[21,3,21,5] : ()
      = std/core/console/printsln("hi");
    val _[21,3,21,5] : ()
      = std/core/console/printsln("hi");
    val _@0@0[21,3,21,5] : ()
      = std/core/console/printsln("hi");
    std/core/console/printsln("hi");
  };
fun example1-expanded // inline size: 4
  = fn<<(std/core/console/console :: X)>>(){
    val _@0[21,3,21,5] : ()
      = std/core/console/printsln("hi");
    val _[21,3,21,5] : ()
      = std/core/console/printsln("hi");
    val _@0@0[21,3,21,5] : ()
      = std/core/console/printsln("hi");
    std/core/console/printsln("hi");
  };
fun example1-fully-expanded // inline size: 4
  = fn<<(std/core/console/console :: X)>>(){
    val _@0[21,3,21,5] : ()
      = std/core/console/printsln("hi");
    val _[21,3,21,5] : ()
      = std/core/console/printsln("hi");
    val _@0@0[21,3,21,5] : ()
      = std/core/console/printsln("hi");
    std/core/console/printsln("hi");
  };
fun example2 // inline size: 4
  = fn<<(std/core/console/console :: X)>>(){
    std/core/list/map<std/core/types/int,(),<(std/core/console/console :: X)>>((std/core/list/#range/list(1, 10)), (fn<<(std/core/console/console :: X)>>(x: std/core/types/int){
        (std/core/console/printsln((std/core/int/show(x))));
      }));
  };
fun example2-expanded // inline size: 4
  = fn<<(std/core/console/console :: X)>>(){
    std/core/list/map<std/core/types/int,(),<(std/core/console/console :: X)>>((std/core/list/#range/list(1, 10)), (fn<<(std/core/console/console :: X)>>(x: std/core/types/int){
        (std/core/console/printsln((std/core/int/show(x))));
      }));
  };
inline borrow "^" fun @val-ask/@select // inline size: 0
  = forall<(e :: E),a> fn(hnd: (ask :: (E, V) -> V)<(e :: E),a>){
    (match (hnd) {
      ((@skip learn/with/@Hnd-ask((@pat@0: std/core/types/int) : std/core/types/int, (@val-ask@0: (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>) : (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>) : (ask :: (E, V) -> V)<(e :: E),a> ) as @pat: ((ask :: (E, V) -> V)<(e :: E),a>))
         -> @val-ask@0;
    });
  };
inline fun @val-ask // inline size: 5
  = fn<<(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>>>(){
    std/core/hnd/@perform0<std/core/types/int,<(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>>,(ask :: (E, V) -> V)>((std/core/types/@open<(std/core/types/total :: E),<(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>>,(i : std/core/hnd/ev-index) -> (std/core/hnd/ev :: ((E, V) -> V) -> V)<(ask :: (E, V) -> V)>,(i : std/core/hnd/ev-index) -> <(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>> (std/core/hnd/ev :: ((E, V) -> V) -> V)<(ask :: (E, V) -> V)>>((std/core/hnd/@evv-at<(ask :: (E, V) -> V)>))((std/core/types/@open<(std/core/types/total :: E),<(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>>,(i : std/core/types/int) -> std/core/types/ssize_t,(i : std/core/types/int) -> <(std/core/types/handled1 :: ((E, V) -> V) -> X)<(ask :: (E, V) -> V)>> std/core/types/ssize_t>(std/core/types/@make-ssize_t)(0)))), learn/with/#@val-ask/@select);
  };
inline borrow "^" fun ask/@val-ask // inline size: 0
  = forall<(e :: E),a> fn(ask@0: (ask :: (E, V) -> V)<(e :: E),a>){
    (match (ask@0) {
      ((@skip learn/with/@Hnd-ask((@pat@0: std/core/types/int) : std/core/types/int, (@x: (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>) : (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>) : (ask :: (E, V) -> V)<(e :: E),a> ) as @pat: ((ask :: (E, V) -> V)<(e :: E),a>))
         -> @x;
    });
  };
inline borrow "^" fun ask/@cfc // inline size: 0
  = forall<(e :: E),a> fn(ask@0: (ask :: (E, V) -> V)<(e :: E),a>){
    (match (ask@0) {
      ((@skip learn/with/@Hnd-ask((@x: std/core/types/int) : std/core/types/int, (@pat@0: (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>) : (std/core/hnd/clause0 :: (V, (E, V) -> V, E, V) -> V)<std/core/types/int,(ask :: (E, V) -> V),(e :: E),a>) : (ask :: (E, V) -> V)<(e :: E),a> ) as @pat: ((ask :: (E, V) -> V)<(e :: E),a>))
         -> @x;
    });
  };